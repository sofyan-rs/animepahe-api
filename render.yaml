services:
  - type: web
    name: animepahe-api
    env: node                 # FIXED: use 'env' instead of 'runtime'
    branch: main
    numInstances: 1
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production